# Correct the rejected Choice

Your submission was rejected for the reasons in the supplied data. It did not
complete this activation and did not dispatch another node. Select an available
Choice by its exact origin/id and provide only its declared string fields, with
all required fields present. Correct the result rather than repeating completed
task work. The engine fails the run on the third combined invalid/missing result.
